# hmis-standards-csv-2-sql
This repo converts the CSV file with the HMIS data standards and converts it to a set of sql tables than can be joined with an extraction so you can improve the documentation.
